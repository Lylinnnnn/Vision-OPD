# Res-OPD Datasets

Dataset adapters and image-path compatibility helpers live here.

- `res_opd_sft_dataset.py`: multimodal SFT dataset wrapper for low-res sampled SFT.

The older PPO/RKL dataset entrypoint is still `res-opd/res_opd_dataset.py` for compatibility with existing training commands.
